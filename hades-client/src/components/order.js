class Order {
    constructor(price, size, ticker, bid, type) {
      this.price = price;
      this.size = size;
      this.ticker = ticker;
      this.bid = bid;
      this.type = type;
    }
}

export default Order;
